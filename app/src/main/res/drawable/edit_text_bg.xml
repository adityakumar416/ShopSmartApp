<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Transparent background -->
    <corners android:radius="24dp" /> <!-- Adjust the radius for rounded corners -->
    <stroke android:width="1dp" android:color="@android:color/darker_gray" /> <!-- Outline color -->
</shape>
